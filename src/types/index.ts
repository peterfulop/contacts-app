export enum ButtonVariant {
  FLAT = 'FLAT',
  BORDERED = 'BORDERED',
}

export enum Theme {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
}

export enum InputState {
  DEFAULT = 'DEFAULT',
  ACTIVE = 'ACTIVE',
  HOVER = 'HOVER',
}

export enum ContactFormAction {
  UPDATE = 'update',
  ADD = 'add',
  DELETE = 'delete',
}
